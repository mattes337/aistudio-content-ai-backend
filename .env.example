# Node Environment
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# Database Configuration (matches docker-compose.db.yml defaults)
DATABASE_URL=postgresql://content_ai_user:content_ai_password@localhost:5432/content_ai_db

# JWT Configuration (must match frontend VITE_API_TOKEN for local dev)
JWT_SECRET=your-jwt-secret-change-in-production

# AI Service Configuration
GEMINI_API_KEY=your-gemini-api-key-from-google-ai-studio

# AI Model Configuration (optional - defaults shown)
# Base model names - override to use different Gemini models
# AI_MODEL_FLASH=gemini-2.5-flash
# AI_MODEL_PRO=gemini-2.5-pro

# Task-specific model overrides (optional)
# These override the base models for specific task categories
# AI_MODEL_RESEARCH=gemini-2.5-pro     # Research/RAG queries
# AI_MODEL_AGENT=gemini-2.5-pro        # Multi-step agent tasks
# AI_MODEL_REFINE=gemini-2.5-flash     # Content refinement
# AI_MODEL_METADATA=gemini-2.5-flash   # Title, subject, excerpt, SEO
# AI_MODEL_IMAGE=gemini-2.5-flash      # Image generation
# AI_MODEL_BULK=gemini-2.5-pro         # Bulk content generation

# Anthropic API Configuration (for Claude Agent SDK)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Tavily API Configuration (for web search in research queries)
# Get your API key from https://tavily.com
TAVILY_API_KEY=your-tavily-api-key

# Open Notebook Configuration (for RAG/Knowledge Base)
OPEN_NOTEBOOK_URL=http://localhost:5055
OPEN_NOTEBOOK_PASSWORD=your-open-notebook-password
OPEN_NOTEBOOK_ENABLED=false

# Research Webhook Configuration (optional)
# When configured, research requests are forwarded to this webhook instead of using the built-in workflow
# The webhook should accept POST requests and return SSE streams for streaming endpoints
# RESEARCH_WEBHOOK_URL=https://your-research-service.example.com/api/research
